target/
metadata.db
metadata.db/
.env
.git/
.idea/
# Logs (any *.log, not just the old run.log name) — keeps stray logs out of the build context.
*.log
# Repo content the build never COPYs — excluded to shrink the context sent to the daemon.
docs/
examples/
tests/
.claude/
.github/
rclone/
