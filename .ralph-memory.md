# Ralph Review Memory

## Failed approaches / dead ends
- None yet in this iteration.

## Notes for next iteration
- C1 (stale VFS links after repair) is a known, documented design trade-off — not a bug to fix
- C2 (seek overflow for >9.2 EB files) is purely theoretical, media files never reach this size
- S1 (include_str! structural tests) are intentional "structural regression guards" — don't remove
- S2 (clean_name allocations) is not a bottleneck given TMDB rate limiting dominates
- I2 (unbounded seen_torrents) is pruned each scan cycle, not a real issue
- The `IncompleteMessage` string check in main.rs is a known hyper 1.x limitation — not fixable without upstream API support
- The duplicated fetch_with_retry logic between rd_client.rs and tmdb_client.rs is deliberate (different API characteristics)
- The `is_video_file` filtering of "sample"/"bonus" word boundaries is a known acceptable trade-off
